export type D3Coord = { x: any; y: any };
export interface Margins {
  top: number,
  bottom: number,
  left: number,
  right: number
}

export interface Data {
  date_time: string,
  portfolio_id: number,
  value: number,
}